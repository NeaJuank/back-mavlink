# PostgreSQL (mismo nombre y usuario que en docker-compose para poder usar Postgres en Docker con backend local)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=drones
DB_USER=dronix_user
DB_PASSWORD=DronixSecure2024!

# Database URL para Python (backend local conectando a Postgres en localhost)
DB_URL=postgresql://dronix_user:DronixSecure2024!@localhost:5432/drones

# MAVLink Configuration
MAVLINK_DEVICE=/dev/ttyACM0
MAVLINK_BAUD=57600
MAVLINK_BAUD=57600
API_HOST=0.0.0.0
API_PORT=8000
LOG_LEVEL=INFO

# Server
PORT=8000
